const Header = {
  render(): string {
    const view = `
        <div class="header-logo"></div>
        <div class="header-menu">
          <h2>Игрушки</h2>
        </div>
        `
    return view;
  }
}

export default Header;